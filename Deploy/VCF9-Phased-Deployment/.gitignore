# Terraform
*.tfstate
*.tfstate.backup
.terraform/
*.auto.tfvars
.terraform.lock.hcl
*.tfvars
.state/
*.backup

# Script 1 JSON output file — contains infrastructure topology
scripts/base_infra_provisioning/outputs.json

# Credentials and sensitive data
*.pem
*.key

# OS files
.DS_Store
Thumbs.db

# Phase 3 generated spec files (contain env-specific IDs/IPs)
Phase_3_VCF9/bringup_spec.json
Phase_3_VCF9/edge_cluster_spec.json

# Phase 2 config and state (contain env-specific settings)
Phase_2_evs_env/python/config.json
Phase_2_evs_env/python/state.json
